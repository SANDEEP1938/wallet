import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    headerTitleStyle: {
        fontSize: 18,
        paddingTop: 5,
        height: 40
    },
    rightText: {
        fontSize: 18,
        color: '#0f0f0f',
        fontFamily: 'Space Grotesk',
        fontWeight: 'bold'
    }
});
export { styles }